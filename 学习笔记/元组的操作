一、元组的操作    # 元组是一旦创建不可变的序列
    a=('a','b',['a','b'])      #创建一个元组包含两个值
    print(a[0])      #与列表相同通过索引位置读取列表的值
    a[2][1]=10     #元组不可以修改但是可以修改元组中元素的列表或字典中的内容


二、元组的常见标准方法
    len()   # 元组的的长度
    max()      #元组中的最大元素
    min()   #元组中的最小元素
    tuple()     #将序列转化为元组


    