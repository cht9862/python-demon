一、文档字符串
    help()     #用来获取方法的说明
    示例：
        help(print)
    
    创建函数的时候，尽量需要定义说明：
        如何定义说名:
            '''
            这是一个文档字符串的示例
            需要注意的是，说明必须卸载函数的第一行
            函数的作用：
            名称：
            参数：
            '''